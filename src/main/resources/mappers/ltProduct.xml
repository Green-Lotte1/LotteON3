<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.kr.lotteon.mapper.LtProductMapper">

	<!-- Prod_List_Test -->
	<select id="selectProducts" resultType="co.kr.lotteon.dto.LtProductDTO">
		SELECT * FROM `lt_product`
	</select>

	<select id="selectProduct" resultType="co.kr.lotteon.dto.LtProductDTO">
		SELECT * FROM `lt_product` WHERE `prodNo` = #{prodNo}
	</select>

	<insert id="insertLtProduct">
		INSERT INTO `lt_product` SET
		`prodCate1`= #{prodCate1},
		`prodCate2`= #{prodCate2},
		`prodName`= #{prodName},
		`descript`= #{descript},
		`company`= #{company},
		`seller`= #{seller},
		`price`= #{price},
		`discount`=#{discount},
		`point`=#{point},
		`stock`=#{stock},
		`delivery`=#{delivery},
		`thumb1`=#{thumb1},
		`thumb2`=#{thumb2},
		`thumb3`=#{thumb3},
		`detail`=#{detail},
		`status`=#{status},
		`duty`=#{duty},
		`receipt`=#{receipt},
		`bizType`=#{bizType},
		`origin`=#{origin},
		`ip`=#{ip},
		`rdate`=NOW();
	</insert>

	<delete id="deleteLtProduct" parameterType="co.kr.lotteon.dto.LtProductDTO">
		DELETE FROM `lt_product` WHERE prodNo=#{prodNo}
	</delete>

</mapper>
